# SPA-фолбэк реализован через dist/404.html (см. scripts/spa-404.mjs), поэтому
# catch-all правило здесь НЕ нужно: Cloudflare отвергал `/* /index.html 200`
# как бесконечный цикл (он сам обрезает .html/index). Файл оставлен пустым
# намеренно — при необходимости сюда можно добавить обычные редиректы.
