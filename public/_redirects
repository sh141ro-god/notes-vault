# SPA-фолбэк для Cloudflare Pages: react-router использует History API, поэтому
# прямой заход/refresh на /notes/<id>, /tasks/<id> и т.п. должен отдавать index.html
# (статус 200, без редиректа). Существующие файлы (assets, sw.js, иконки) отдаются
# как есть — это правило срабатывает только когда файла нет.
/*    /index.html   200
